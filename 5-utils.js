const sayHi = (name) => {
    console.log(`Hellow there ${name}`);
};

// {use, an, object, if, you, have, multiple, variables, or. things} 
// or just set one thing equal to modules.exports
module.exports = sayHi; 
// similar to "export default" in es6 client-side javascript.
